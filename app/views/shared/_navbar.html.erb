<nav class="navbar navbar-expand-lg bg-dark sticky-top" data-bs-theme="dark">
  <div class="container">
    <a class="navbar-brand" href="#">Inmobiliaria Virtual</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto">
        <%= link_to "Home", root_path, class:"nav-link active", ariaCurrent:"page" %>
        <%= link_to "Edificios", buildings_path, class:"nav-link" %>
        <%= link_to "Departamentos", apartments_path, class:"nav-link" %>
        <% if user_signed_in? && current_user.admin? %>
          <%= link_to "Características", features_path, class:"nav-link" %>
        <% end %>
      </div>
      <div class="dropdown">
        <a class="btn btn-secondary dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <% if !user_signed_in? %>
            Ingresar / Registrarse
          <% elsif %>
            Hola <%= current_user.nombre %>!
          <% end %>
        </a>
        <ul class="dropdown-menu">
          <% if !user_signed_in? %>
              <li><%= link_to "Ingresar", new_user_session_path, class:"dropdown-item" %></li>
              <li><%= link_to "Registrase", new_user_registration_path, class:"dropdown-item" %></li>
          <% elsif %>
              <li><%= link_to "Mi Cuenta", edit_user_registration_path, class:"dropdown-item" %></li>
              <li><%= button_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class:"dropdown-item", form: {data: {turbo_confirm: "Estás seguro que deseas cerrar tu sesión?"} } %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>